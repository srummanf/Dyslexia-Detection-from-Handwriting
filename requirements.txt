# Runtime dependencies for the Streamlit app.
# For notebook training extras, also: pip install -e ".[nlp,ocr,vision,explain,dev]"

# --- core / tabular model ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
pyyaml>=6.0
pillow>=10.0

# --- app ---
streamlit>=1.49
plotly>=5.20
matplotlib>=3.8

# --- linguistic feature extraction (offline) ---
pyspellchecker>=0.8
jellyfish>=1.0

# optional: real grammar checking (needs a JRE). Falls back to a heuristic if absent.
# language-tool-python>=2.8

# --- OCR (offline). Heavy: pulls in torch. Comment out to run app in "features off" mode. ---
easyocr>=1.7

# --- image models. Needed only if you ship the YOLO / ConvNeXt classifiers. ---
ultralytics>=8.2
timm>=1.0
